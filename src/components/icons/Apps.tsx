import styled from 'styled-components';
import { Apps as Apps_ } from 'styled-icons/material';

import { sharedStyle } from './utils';

const Apps = styled(Apps_)`
  ${sharedStyle}
`;

export default Apps;
