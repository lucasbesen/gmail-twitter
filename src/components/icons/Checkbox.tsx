import { Checkbox as Checkbox_ } from 'styled-icons/boxicons-regular/Checkbox';
import styled from 'styled-components';

import { sharedStyle } from './utils';

const Checkbox = styled(Checkbox_)`
  ${sharedStyle}
`;

export default Checkbox;
