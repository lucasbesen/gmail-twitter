import { Refresh as Refresh_ } from 'styled-icons/material/Refresh';
import styled from 'styled-components';

import { sharedStyle } from './utils';

const Refresh = styled(Refresh_)`
  ${sharedStyle}
`;

export default Refresh;
