import styled from 'styled-components';
import { Menu as Menu_ } from 'styled-icons/material';

import { sharedStyle } from './utils';

const Menu = styled(Menu_)`
  ${sharedStyle}
`;

export default Menu;
