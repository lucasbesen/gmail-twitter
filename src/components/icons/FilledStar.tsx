import { Star as FilledStar_ } from 'styled-icons/material/Star';
import styled from 'styled-components';

import { sidebarIconSharedStyle } from './utils';

const FilledStar = styled(FilledStar_)`
  ${sidebarIconSharedStyle}
`;

export default FilledStar;
