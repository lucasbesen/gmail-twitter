import { StarBorder as Star_ } from 'styled-icons/material/StarBorder';
import styled from 'styled-components';

import { sharedStyle } from './utils';

const Star = styled(Star_)`
  ${sharedStyle}
`;

export default Star;
