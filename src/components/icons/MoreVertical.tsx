import { MoreVert as MoreVertical_ } from 'styled-icons/material/MoreVert';
import styled from 'styled-components';

import { sharedStyle } from './utils';

const MoreVertical = styled(MoreVertical_)`
  ${sharedStyle}
`;

export default MoreVertical;
